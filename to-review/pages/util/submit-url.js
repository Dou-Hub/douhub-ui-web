import _ from '../../../util/web';

const MainArea = (props) => {
    return null;
}

const getServerSideProps = async (props) => {
    const { query } = props;
    return {}
}

export const submitUrlPage = { MainArea, getServerSideProps };